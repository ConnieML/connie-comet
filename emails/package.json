{
  "name": "connie-emails",
  "version": "1.0.0",
  "description": "Connie email templates and sending scripts",
  "private": true,
  "scripts": {
    "dev": "email dev --dir templates --port 3001",
    "preview": "email dev --dir templates --port 3001",
    "export": "email export --dir templates --outDir out",
    "build": "email build --dir templates"
  },
  "dependencies": {
    "@react-email/components": "^0.5.7",
    "react": "^19.1.0",
    "react-email": "^4.3.2",
    "resend": "^6.4.1"
  },
  "devDependencies": {
    "@react-email/preview-server": "4.3.2",
    "@types/react": "^19.1.0",
    "typescript": "^5.7.3"
  }
}
